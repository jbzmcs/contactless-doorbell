#ifndef BUZZER_H
#define BUZZER_H

// Initializes the buzzer pin as an output.
void buzzer_init(void);

// Turns the buzzer on (sets the pin HIGH).
void buzzer_on(void);

// Turns the buzzer off (sets the pin LOW).
void buzzer_off(void);

#endif // BUZZER_H